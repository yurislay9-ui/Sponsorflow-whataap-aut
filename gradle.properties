# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *should update*
# this file.

# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
# Optimización del Daemon para evitar crasheos en el workspace
org.gradle.jvmargs=-Xmx4g -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8 -XX:+UseG1GC

# Control de paralelismo de Kotlin (vital para que KSP no asfixie el CPU)
kotlin.daemon.jvm.options=-Xmx2g
org.gradle.parallel=true
org.gradle.workers.max=2

# Habilitar caché estricto para no recompilar los módulos de SRE
org.gradle.caching=true

# AndroidX Package Structure - REQUIRED for all modern Android projects
android.useAndroidX=true

# Enables namespacing of each library's R class so that its R class includes only the
# resources declared in the library itself and none from the library's dependencies,
# thereby reducing the size of the R class for that library
android.nonTransitiveRClass=true